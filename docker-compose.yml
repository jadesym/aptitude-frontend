version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
    environment:
      - PORT
    init: true
    ports:
      - "${PORT}:${PORT}"
    tty: true
